import React from "react";
import ReactDOM from "react-dom";
import Section from "./Components/Section";

const rootElement = document.getElementById("root");
ReactDOM.render(<Section />, rootElement);
